#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int count;
	for (count=0; 100-count>=0;count+=2)
	{
	printf("So %d\n",100-count);
}
	return 0;
}
